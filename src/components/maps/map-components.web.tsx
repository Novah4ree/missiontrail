import { View } from 'react-native';

// Web previews use harmless views because react-native-maps contains native-only code.
export const MapView = View;
export const Marker = View;
export const Polyline = View;
export const Circle = View;
export const PROVIDER_GOOGLE = undefined;
