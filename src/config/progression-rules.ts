/**
 * Explorer-level curve shared by every client screen.
 *
 * Permanent XP still comes from the Supabase ledger. These values only convert
 * that lifetime total into a display level and can be tuned in one place.
 */
export const PLAYER_LEVEL_XP_BASE = 100;
export const PLAYER_LEVEL_XP_EXPONENT = 1.35;
